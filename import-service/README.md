# Import Service

AWS CDK service for CSV product import.

## Useful commands

* `npm run build` compile TypeScript
* `npx cdk synth` synthesize the CloudFormation template
* `npx cdk deploy` deploy the stack
